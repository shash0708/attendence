import { Button, ButtonGroup } from '@chakra-ui/react'

import React from 'react'

const ButtonProp = (props) => {
  return (
    <div>
    <Button colorScheme='blue'>{props}</Button>
    </div>
  )
}

export default ButtonProp;
